generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  //! UnComment below 2 lines before migrating to db and generating types to ts ..  
  // url       = env("DATABASE_URL")
  // directUrl = env("DIRECT_URL")
}

// Defining the Schemas 

model User {
  id                            String         @id @default(uuid())
  name                          String
  email                         String         @unique
  conversations                 Conversation[]
  password                      String
  // Verification
  isVerified                    Boolean        @default(false)
  emailVerificationToken        String?
  emailVerificationTokenExpires DateTime?
  // Reset Password
  resetPasswordToken            String?
  resetPasswordExpires          DateTime?
  // On Login - For now only supports a single device login 
  refreshToken                  String?
  refreshTokenExpires           DateTime?
  // User CreatedAT :
  createdAt                     DateTime       @default(now())
}

model Conversation {
  id        String    @id @default(uuid())
  title     String
  userId    String
  messages  Message[]
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Message {
  id             String       @id @default(uuid())
  content        String
  role           MessageRole
  sources        String[]     @default([]) // Array of source URLs or references generated by the assistant(collected from the web)
  conversationId String
  createdAt      DateTime     @default(now())
  conversation   Conversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
}

enum MessageRole {
  User
  Assistant
}
